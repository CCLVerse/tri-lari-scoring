version: "3.9"

services:
  tri-lari-scoring:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8787:8787"
    environment:
      PASSWORD: rstudio
    volumes:
      - ./:/home/rstudio/workspace